# API Configuration
# For local development, use http://localhost:3000
# For Docker Compose, the frontend container will use the backend service directly
# This file is only used for local development with 'npm run dev'
VITE_API_URL=http://localhost:3000
